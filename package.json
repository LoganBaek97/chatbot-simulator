{
  "name": "chatbot-simulator",
  "version": "1.0.0",
  "description": "Chatbot simulation application with React frontend and Express backend",
  "scripts": {
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "dev": "concurrently \"cd server && node server.js\" \"cd client && npm run dev\"",
    "install-all": "cd client && npm install && cd ../server && npm install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
